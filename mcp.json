{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "codebuddy": {
      "type": "stdio",
      "command": "./bin/codebuddy-mcp-server.py",
      "cwd": "${PLUGIN_ROOT}",
      "env": {
        "MCP_SERVER_REQUEST_TIMEOUT": "3600000",
        "MCP_REQUEST_TIMEOUT": "3600",
        "MCP_CONNECTION_TIMEOUT": "3600",
        "MCP_TIMEOUT": "3600",
        "MCP_MAX_REQUEST_TIMEOUT": "3600000",
        "MCODE_BASE_PROMPT_FILE": "${PLUGIN_ROOT}/assets/mcode-base-system-prompt.md",
        "MCP_STATE_DIR": "${PLUGIN_DATA}/state"
      }
    }
  }
}
