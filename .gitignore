# Runtime state (regenerated on every plugin invocation)
state/
logs/

# Build / cache artifacts
__pycache__/
.pytest_cache/
*.pyc
*.pyo
*.pyd

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# Tool state (codebuddy CLI's own runtime dir, not this plugin's)
.codebuddy/

# Client-generated manifest (regenerated by `mcode install`; per AGENTS.md "don't hand-edit it")
.minimax-plugin/

# Long-prompt test artifact (the test asks codebuddy to write an LRU cache implementation
# into a real file, which is the test's prompt; the file is test output, not source)
lru_cache.py
